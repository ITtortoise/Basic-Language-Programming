#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long int t;
    string s;
    getline(cin,s);
    {
        sort(s.begin(),s.end());
        while(next_permutation(s.begin(),s.end()));
            cout << s << '\n';

    }

    return 0;
}
