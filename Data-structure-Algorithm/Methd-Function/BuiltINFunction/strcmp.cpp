//Example reads in two strings (w/out spaces) and compares them for equality
#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s1,s2;
    cin>>str1;
    cin>>str2;
    if(!strcmp(str1, str2)
    {
        cout<<"Strings are equal!";
    }
}
